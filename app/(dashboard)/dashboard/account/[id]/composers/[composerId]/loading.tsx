import { EditorSkeleton } from '@/components/ui/skeletons';

/**
 * Composer Editor Loading State
 *
 * Shows skeleton UI while composer detail page loads.
 */
export default function ComposerEditorLoading() {
  return <EditorSkeleton />;
}
