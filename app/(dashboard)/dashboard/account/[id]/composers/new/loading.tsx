import { EditorSkeleton } from '@/components/ui/skeletons';

/**
 * New Composer Loading State
 *
 * Shows skeleton UI while the new composer form loads.
 */
export default function NewComposerLoading() {
  return <EditorSkeleton />;
}
