import { EditorSkeleton } from '@/components/ui/skeletons';

/**
 * Work Editor Loading State
 *
 * Shows skeleton UI while work detail page loads.
 */
export default function WorkEditorLoading() {
  return <EditorSkeleton />;
}
