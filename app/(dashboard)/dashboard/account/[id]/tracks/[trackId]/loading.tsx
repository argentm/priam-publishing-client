import { EditorSkeleton } from '@/components/ui/skeletons';

/**
 * Track Editor Loading State
 *
 * Shows skeleton UI while track detail page loads.
 */
export default function TrackEditorLoading() {
  return <EditorSkeleton />;
}
