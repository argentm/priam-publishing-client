import { EditorSkeleton } from '@/components/ui/skeletons';

/**
 * New Track Loading State
 *
 * Shows skeleton UI while the new track form loads.
 */
export default function NewTrackLoading() {
  return <EditorSkeleton />;
}
