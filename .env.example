# =============================================================================
# Priam Publishing Client - Environment Variables
# =============================================================================
# Copy this file to .env.local for local development
# For production (DigitalOcean), set these in the App Platform dashboard
#
# IMPORTANT: NEXT_PUBLIC_* variables must be set at BUILD TIME
# In DigitalOcean: Set these in the "App-Level Environment Variables" section
# so they're available during the build phase.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (BUILD TIME - NEXT_PUBLIC_*)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_API_URL (BUILD TIME)
# Used by Next.js rewrites to proxy /api/* requests to backend
# MUST be set during build - rewrites are compiled into the build output
# Local: http://localhost:3001
# Production: https://your-server-app.ondigitalocean.app
NEXT_PUBLIC_API_URL=http://localhost:3001

# API_URL (RUNTIME)
# Used by server-side routes for direct backend API calls
# Can be set at runtime (component-level env vars in DigitalOcean)
# Local: http://localhost:3001
# Production: Same as NEXT_PUBLIC_API_URL (or internal URL if available)
API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Site URL (RUNTIME fallback)
# -----------------------------------------------------------------------------
# Used as fallback when headers aren't available
# Local: http://localhost:3000
# Production: https://your-client-app.ondigitalocean.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Stripe Configuration
# -----------------------------------------------------------------------------
# Get from: https://dashboard.stripe.com/test/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
